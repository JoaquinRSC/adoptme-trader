name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build and package
        run: npm run package:win

      - name: Find packaged artifacts
        id: find_artifacts
        shell: pwsh
        run: |
          $exe = Get-ChildItem -Path "dist/electron/Packaged" -Filter "*.exe" -Recurse | Select-Object -First 1
          $yml = Get-ChildItem -Path "dist/electron/Packaged" -Filter "latest.yml" -Recurse | Select-Object -First 1
          echo "EXE_PATH=$($exe.FullName)" >> $env:GITHUB_OUTPUT
          echo "YML_PATH=$($yml.FullName)" >> $env:GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ steps.find_artifacts.outputs.EXE_PATH }}
            ${{ steps.find_artifacts.outputs.YML_PATH }}
          generate_release_notes: true
          draft: false
